export CUSTOM_TOOL_DIR="$HOME/store/projects/dev/custom-tools"

pathes=(
    "$HOME/bin"
    "$HOME/.local/bin"
    "$HOME/.cargo/bin"
    "$HOME/go/bin"
)

for p in $pathes; do
    export PATH=$PATH:$p
done
